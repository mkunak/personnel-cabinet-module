const loginRoutes = require("./login");
const resetRoutes = require("./reset");
const logoutRoutes = require("./logout");
const registerRoutes = require("./register");
const newPasswordRoutes = require("./new-password");

module.exports = {
  loginRoutes,
  resetRoutes,
  logoutRoutes,
  registerRoutes,
  newPasswordRoutes,
};
